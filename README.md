# Too Long, Might Read

A CLI tool that uses GPT4 to automatically generate summaries of scientific papers, formatted in the [IMRaD](https://en.wikipedia.org/wiki/IMRAD) structure, by simply adding PDFs of the papers to a user-specified directory. The TLMR tool will output the summaries in a user-specified directory, the default is `summaries`.
